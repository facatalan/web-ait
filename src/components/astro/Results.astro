---
const levels = [
  {
    level: 1,
    title: 'SEMANA 1-2: Dominio Práctico',
    color: 'accent-blue',
    items: [
      'Reduces de 4 horas a 45 minutos la preparación de una presentación ejecutiva',
      'Generas análisis de datos complejos que antes delegabas',
      'Produces contenido en 1/5 del tiempo (sin que suene a IA)',
    ],
  },
  {
    level: 2,
    title: 'SEMANA 3-4: Amplificación Estratégica',
    color: 'accent-purple',
    items: [
      'Diseñas sistemas de IA personalizados para tus procesos recurrentes',
      'Conviertes tu experiencia en "gemelos digitales" consultables',
      'Automatizas decisiones repetitivas sin perder criterio profesional',
    ],
  },
  {
    level: 3,
    title: 'SEMANA 5-6: Transformación Cognitiva',
    color: 'accent-cyan',
    items: [
      'Resuelves problemas complejos usando IA como co-pensador estratégico',
      'Desarrollas ventajas competitivas difíciles de replicar',
      'Con la IA logras cosas que solo el top 1% de los que usan IA pueden lograr',
    ],
  },
];
---

<section class="py-12 bg-dark-900">
  <div class="max-w-6xl mx-auto px-6">
    <div class="flex items-center gap-4 mb-16">
      <span class="text-accent-cyan font-mono text-sm">05</span>
      <div class="h-px flex-1 bg-gradient-to-r from-accent-cyan/50 to-transparent"></div>
    </div>

    <h2 class="text-4xl md:text-5xl font-bold mb-16 text-center">
      Lo que lograrás con <span class="gradient-text">AI-Thinking</span>
    </h2>

    <div class="grid md:grid-cols-3 gap-8">
      {levels.map((level) => (
        <div class="relative p-8 bg-dark-800/50 rounded-2xl border border-white/5 overflow-hidden">
          <!-- Level badge -->
          <div class={`absolute top-4 right-4 w-12 h-12 rounded-full bg-${level.color}/20 flex items-center justify-center`}>
            <span class={`text-${level.color} font-bold`}>{level.level}</span>
          </div>

          <h3 class="text-xl font-bold mb-6 pr-12">{level.title}</h3>

          <ul class="space-y-4">
            {level.items.map((item) => (
              <li class="flex items-start gap-3">
                <svg class="w-5 h-5 text-green-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span class="text-gray-300">{item}</span>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>
